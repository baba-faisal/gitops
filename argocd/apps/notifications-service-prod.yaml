apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notifications-service-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/baba-faisal/gitops.git
    targetRevision: main
    path: helm/microservice
    helm:
      valueFiles:
        - ../values/notifications-service-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    syncOptions:
      - CreateNamespace=true      # <-- this replaces kubectl create namespace
    automated:
      prune: true
      selfHeal: true
